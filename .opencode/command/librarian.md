---
description: Run the librarian umbrella loop (health → ingest/verify → gh).
agent: librarian
---

Run the librarian umbrella per `skills/librarian/SKILL.md`. Default `health`; pass `ingest <path>` or `verify` as args.

$ARGUMENTS

