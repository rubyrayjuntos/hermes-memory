# Level-6 pane pass — 2026-09-07 (OC-MVP-4 / OC-MVP-3)

Process: `PYTHONPATH=src python -m hermes_memory.graph_api` from
`/home/rswan/Documents/hermes-memory` @ `ba2fd9d` (`wip/oc-mvp-4`), then a
one-line Fountain fix so an empty DISSOLVE does not wipe the funnel.
Bind `127.0.0.1:7890`.

This is **not** a Met flip. User-facing rows stay Open until this artifact is
reviewed and the remaining holes below are closed.

## DSN (checked before the pass)

`#74` / fail-loud **is on `origin/main`** (`207e635`, merge of #76). This
checkout includes that commit. `load_config` in `src/hermes_memory/config.py`
contains `Refusing silent fallback`. `dotenv_paths` loads the clone `.env`
first.

Clone DSN shape (values not printed): `postgresql` / `127.0.0.1` / `5450` /
`hermes_memory`. Password is not the `***` placeholder.

Restarted pane from this clone (not the installed pin). Live proof this is
**Documents `:5450`**, not installed `:5452` (4 turns):

| Source | Fact |
|--------|------|
| `GET /api/health` | `drain_status.complete=596` `unset=264` `graph_degraded=9` `unpassported_count=266` |
| `GET /api/librarian/graph/stats` | `manifold.turns=869` `nouns=1902` `mentions=2093` |
| Fountain status | `live · … drain_status graph_degraded=9 · unpassported=266` |
| Fountain metrics | `MANIFOLD 869` / `869 turns · 1902 nouns · 2093 mentions` |

If this had been the installed DB, manifold would be 4/9/7.

## `/hop` consumers (grep)

Only these paths mention the route:

- `src/hermes_memory/graph_view.py` (`match_route` → `noun_hop`)
- `src/hermes_memory/graph_http.py` (GET handler)
- `src/hermes_memory/graph_runtime.py` (`noun_hop` / `_anoun_hop`)
- `docs/graph/fountain.html` (click fetch)
- `tests/test_graph_api.py`

No other in-repo consumer.

## §1 Hover

Synthetic `pointermove` on the canvas after a search left this tooltip body
(display was `none` — not a clean on-screen hover capture):

`mentions let s → let s start` + `magnitude=0.85 · decay=0.97 · beam_score=0.02`

That is search-context scoring, not an invented catalog score. A **visible**
catalog hover (identity + degree, no score) was **not** captured with
`display:block`. Treat hover as **partial** for this pass.

## §2 Click / 1-hop

Opened `http://127.0.0.1:7890/api/librarian/pane`. Clicked `hermes-memory` and
`Tokyo Eye` in the noun list. `#hook` after fetch (truncated):

```
hermes-memory  type=Noun

Embedding version not recorded (pre-V10) — trusted as nomic-768 by policy, not directly confirmed.

mentions → MEMORY  magnitude=1.7
  turn 375: you have pinpointed exactly what needs improving! …
  turn 376: **Yes — this is the exact boundary.**
```

```
Tokyo Eye  type=Noun

Embedding version not recorded (pre-V10) — trusted as nomic-768 by policy, not directly confirmed.

mentions → am thinking  magnitude=0.7
  turn 292: i am thinking about this in terms of tokyo eye protein topology analysis …
mentions → tokyo eye protein  magnitude=0.85
  turn 292: i am thinking about this in terms of tokyo eye protein topology analysis …
```

`GET /api/librarian/nouns/330/hop` (label `hermes-memory`): 32 neighbors,
`embed_reason` set, first excerpts present. JSON subset:
[`2026-09-07-oc-mvp-4-level6.json`](2026-09-07-oc-mvp-4-level6.json).

## §3 Funnel

First DISSOLVE (`q=hybrid age memory`) returned **0 ANN seeds** (`embed_ms=2.7`,
`vector_ms=0.0` — embed path did not produce candidates). The pane initially
reset the funnel on the empty-search catalog fallback. After the Fountain
fix, DISSOLVE kept:

```
ANN candidates: 0  →  above similarity 0.55: 0  →  after graph expand: 0 (0 seed + 0 new via edges)  →  kept after beam/budget: 0
```

Mode `SEARCH · NO HITS`. That is an honest zero, not a missing readout.

## OC-MVP-3 (d)

Tokyo Eye hop excerpts are readable as “protein topology / residue-edge
thinking.” That is inspector text, not “from the graph alone.” Nightingale
on `:5452` was **not** this pane. **Do not Met `OC-MVP-3`.**

## Status after this pass

| Row | After this artifact |
|-----|---------------------|
| `OC-MVP-4` | Still **Open**. Click + funnel demonstrated on the live `:5450` pane. Hover screenshot/tooltip visibility is incomplete. ANN path returned 0 seeds. |
| `OC-MVP-3` | Still **Open**. Meaning is readable from hop excerpts, not from the 3D layout alone. |

Playwright viewport shots were taken in-session (`click-hop`, `funnel`,
`tokyo-hop`); they did not land in this tree. The transcript + JSON above
are the attached artifacts.
