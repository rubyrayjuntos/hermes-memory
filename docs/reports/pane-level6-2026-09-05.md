# Pane level-5/6 artifact — 2026-09-05

Process: `hermes-memory-api` from `/home/rswan/Documents/hermes-memory/src` (`9428da0`/`85c7dc6` tree) on `127.0.0.1:7890`.
Postgres: compose `hermes-memory-postgres` healthy on `127.0.0.1:5450`.
SHA of docs at time of this capture: working tree includes `DEFINITION_OF_DONE.md` (uncommitted).

## `GET /api/health` (returned, not assumed)

```json
{
  "ok": true,
  "bind": "127.0.0.1:7890",
  "dropped_writes": 0,
  "writes_failed": 0,
  "embed_null": 0,
  "graph_degraded": 0,
  "last_failed_stage": "",
  "last_failed_at": "",
  "drain_status": {
    "complete": 0,
    "embed_null": 0,
    "graph_degraded": 0,
    "unset": 818
  },
  "unpassported_count": 818
}
```

`unset: 818` and `unpassported_count: 818` are the V9-gap fact (rows exist, no passports / no `drain_status` stamp), not ingest `isolated_files`.

## `GET /api/librarian/pane`

HTTP 200, ~56 KB HTML, contains Garden/Cockpit (Fountain).

## `GET /api/librarian/graph/stats` (subset)

- `vertices.total`: 1020
- `manifold.turns`: 818
- `manifold.nouns`: 0
- `manifold.mentions`: 0

## Browser (same session)

Opened `http://127.0.0.1:7890/api/librarian/pane`. Title: Fountain (live manifold).
Status line (not ingest `drifted`): `live · ledger graph_degraded=0 writes_failed=0 embed_null=0 · drain_status graph_degraded=0 · unpassported=818`.
Metrics: MANIFOLD 818 turns / 0 nouns / 0 mentions; catalog empty (“seed it in chat”). Graph viewport empty of gems because the mention mesh is empty (V9-gap), not because the pane failed to load.

## What this is / is not

- **Level 5:** curl bodies above.
- **Level 6:** human-visible pane + status line matching `/api/health`. Artifact: this file + the session screenshot of Fountain.
- **Not** a quality claim: 0 nouns / 0 mentions is the live V9-gap window, not “recall is good.”
- **Not** Met for `OC-MVP-*` in `RELEASE_CRITERIA.md` — CI at `9428da0` still has to be opened. This file only closes the “is it running” loop.
