# Adversarial content

O'Brien's notebook says: don't interpolate `it's` naively.

Backslash soup: C:\Users\o'brien\temp and a regex like r'\w+\s*\' .

Unicode: café naïve — em-dash, 日本語, emoji 🚀, math ∑∫π.

```sql
SELECT * FROM notes WHERE author = 'O''Brien';
```

A stray backtick ` and an unclosed quote ' below —
'never closed
